import 'package:flutter/material.dart';

class JoinPollScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Join Poll'),
      ),
      body: Center(
        child: Text('Join Poll Screen'),
      ),
    );
  }
}